pipeline {
    agent { node { label 'Agent-1' } }
    
    //options {
       // ansiColor('xterm')
    

    stages {
        stage('Init'){
            steps {
                sh'''
                    cd roboshop_infra_standard/01-vpc
                    ls -ltr
                    pwd
                    terraform init
                '''
            }
        }
    }
}